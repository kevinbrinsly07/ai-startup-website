import React, { Component } from "react";
import Nav from "@/components/Nav";
import Hero from "@/components/Hero";

const page = () => {
  return (
    <div>
      <Nav />
      <Hero />
    </div>
  );
};

export default page;
